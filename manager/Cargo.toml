[package]
name = "pde-manager"
edition = "2024"

[dependencies]
home = "0.5"
lexopt = "0.3"
self-replace = "1.5"
serde = { version = "1.0", features = ["derive"] }
toml = { version = "0.8", features = ["preserve_order"] }
which = "8"
